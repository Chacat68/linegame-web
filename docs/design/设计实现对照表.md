# 星际贸易商 - 已实现 / 部分实现 / 未实现清单

> 更新日期：2026-08-21
> 判定基准：以当前仓库 `js/`、`ui/`、`tests/` 实际代码与主循环接线为准，不以历史文档或已删除模块为准。
> 状态说明：`✅ 已实现` = 已接入主循环或运行时可用；`🔶 部分实现` = 有代码但存在明显缺口、占位、未完全接线或只完成骨架；`❌ 未实现` = 设计文档存在，但仓库内没有对应模块或没有运行时能力。

---

## 0. 总览

当前项目已经不是“只有设计文档”的阶段。核心贸易闭环、经济、事件、派系、科技、任务、成就、胜利、舰队、船员、金融、贸易站、存档、3D 星图都已落地。全量经济审计已完成，价格优惠共享递减上限、贸易站回本曲线、五条胜利路线量化门槛均已验收。

本轮（2026-08-21）状态：
- ✅ 中期专题教学链（`MidgameTeachingChain.js`）：4 条专题链已具备主动启动入口、建议提权、真实动作推进、派遣循环基线、单链并发约束和 v17 持久化
- ✅ 本地平衡统计：`BalanceMetricsSystem` 随存档只在本设备记录；设置页可导出脱敏 JSON 审查，不会自动上传，也不再展示无效的采集同意开关
- ✅ 当前版本、存档与测试口径已核对：`GAME_VERSION = 0.6.4`、`SAVE_SCHEMA_VERSION = 17`，全量测试为 231 个文件、1575 项
- ✅ 星图 UI 所有权已拆分：面板 DOM/ARIA/滚动/几何、根节点/委派协议、Context/Renderer selection/Escape、Renderer/EventBus/DOM listener 生命周期、工作区请求/视图切换/旅行分发/引导聚焦分别归 `MapPanelViewController`、`MapPanelController`、`MapContextController`、`MapInteractionController`、`MapNavigationController`；`MapUI` 从 674 行收束为 331 行无 DOM 组合门面

目前真正的缺口主要集中在三类：

1. 设计文档里体量很大的远期系统：完整音频平台、多人联网、真正的 i18n。
2. 已有 MVP 但仍需深化的系统：探索深化（遗迹链/异常点）、飞船高阶能力和剧情演出。
3. 资产与商网的高级调度体验仍不足：贸易站经营虽已并入 `MarketUI.js`，`CommerceFacade.getCommerceSnapshot()` 也已补齐，商网总览也支持批量投资 / 升级 / 经理 / 策略指令，但跨站物流与区域供应链调度仍未展开。

---

## 1. 核心架构与状态管理

### ✅ 已实现

- `js/core/StateSession.js` 持有 state/revision/token；`GameApplication.js` 是正式组合根并持有迁移期 `_state` 镜像与受限 Runtime Graph，`GameManager.js` 仅重导出历史公共 API。
- `js/core/EventBus.js` 已覆盖日志、教程、成就、研究等广播场景。
- `js/data/constants.js` 已成为状态结构与存档契约的唯一真理来源；当前 `SAVE_SCHEMA_VERSION = 17`，`GAME_VERSION = 0.6.4`。
- `SettingsUiController` 永久独占外部 launcher、延迟加载与 session/generation 失效；`SettingsViewPresenter` 纯生成控件/摘要模型，`SettingsModalController` 独占设置弹层分页、控件、结果状态、危险确认和释放；36 行 `SettingsManager.js` 只组合兼容导出。`SettingsCommandController.js` 独占设置 mutation、持久化、Renderer/Audio 投影、难度/时钟通知与危险操作提交；`UsageDataExportEffect.js` 由正式设置命令调用并独占脱敏 JSON 下载与清理。
- `SaveWorkspacePresenter` 纯生成存档安全状态、槽位、迁移区和确认描述；`SaveWorkspaceController` 独占单一根委托、保存/读取/删除确认、JSON 文件导入、Blob 下载、异步失效与释放；保存/读取经 `SaveCommandAdapter` 发布冻结 `SaveCommand` 到 `GamePersistenceController.handleCommand`，18 行 `SaveUI.js` 只组合请求对象 render/diagnostics/reset，并由 Feature manifest 与 UI 会话重置清理。
- `js/core/GameFeatureRuntime.js` 已把 `FeatureRegistry` 与 `GameFeatureManifest` 组合成唯一已配置端口，统一市场、舰队、档案、存档、胜利、设置、教程、引导、经营/路线/成就、剧情和随机事件模块的依赖、按需加载、会话同步、失败重试与释放。
- 434 行 `js/core/GameUiApplicationRuntime.js` 只统一 MarketWorkspace、Settings、GameUiCoordinator、GameUiLifecycle 与 Context adapters 的惰性组装、latest-state/session providers、入口呈现、增量刷新与释放；49 行 `GameUiNavigationPort.js` 独占冻结导航协议，36 行 `GameUiApplicationDiagnostics.js` 纯组合 Coordinator renderer/session、Feature recovery、Context Inspector、Event UI、Navigation、Workspace Surface、共享 L4、Blocking/Escape 与子控制器快照，`GameFeatureRecoveryDiagnostics` 继续只负责 registry、呈现与 Settings 恢复子快照。
- `BlockingSurfaceDismissRegistry.js` 独占每个 Blocking Surface 的单一 backdrop listener、多 owner 仲裁与幂等 token release；`SurfaceManager.js` 只编排焦点、可见性和唯一 document dispatcher，并已删除永久 bind API。Settings 外壳/Feature、HUD 胜利、Dialogue、交易确认、危险确认、公司命名与随机事件均进入完成/取消/session/Feature/application release，最后 owner 离开后不保留 Map 条目或 Detached DOM listener；顶层 diagnostics 只增加 owner 计数，不暴露回调、token 或 DOM。
- 343 行 `EventUI.js` 已拆为 108 行纯 `EventPresenter`、312 行 `EventSurfaceController` 与 66 行兼容门面：事件标签/摘要/影响/选择模型、Blocking DOM/键盘/短视口焦点可见性/listener/token 和 pending 会话分别只有一个 owner；门面不读 DOM，Controller diagnostics 不泄漏事件对象或回调。
- `js/ui/DeferredFeatureStatusUI.js` 已统一市场、机库、档案、存档与设置的 loading/error/retry 呈现；失败时不切走工作区或 Blocking Surface，重试后读取最新 session state。
- `js/core/GameLoopRuntime.js` 已统一实时日、场景帧、延迟功能/DOM/教程暂停、领域日推进与 active-dispatch 周期任务；`GameManager` 不再直接持有 RAF、recurring id 或暂停选择器。
- `js/core/GameApplicationLifecycle.js` 已统一应用级 shutdown 顺序、失败隔离和幂等语义；页面退出与 HMR 共用该入口，Context/Feature/Renderer 均有真实释放路径。
- `js/core/GameRuntimeGraph.js` 已统一 12 个应用 runtime 节点的惰性构造、循环依赖保护、失败重试、generation 诊断与引用清理。
- `js/core/GameStartupProjection.js` 已统一 Settings 读取、启动状态解析、Audio 初始化、Renderer 初始化/设置投影和 release；`GameApplication` 只保留 prepare → session transition → scene → UI 的顶层顺序。
- `js/systems/commerce/CommerceFacade.js` 已作为市场、金融、贸易站动作的统一入口，并被 `GameManager` 实际调用。

### 🔶 部分实现

- `GameManager.js` 已从约 2,900 行收束为 9 行、只重导出 `init / shutdown` 的兼容门面，`main.js` 直接依赖正式 `GameApplication`；`GameApplication` 当前 214 行且生产导出同样只有生命周期入口，12 个节点由 101 行 `GameRuntimeNodeFactories` 薄注册表校验唯一归属，并按 session/feature/action/guidance/UI 拆入五个 78–163 行职责簇；Renderer/Settings/Audio 启动职责迁入 `GameStartupProjection`。7 个应用级集成命令已进入仅 test mode 注册的冻结 `GameApplicationTestHarness`，继续覆盖真实单例 Runtime Graph。
- 存档契约当前已经以 `SAVE_STATE_SCHEMA` 为唯一来源，但这套约束主要体现在代码和测试中，仍需继续靠文档与回归测试来防止后续字段扩张时重新失焦。

### ❌ 未实现

- 统一资源管理器（图片、音频、资产预加载）
- 独立网络同步层
- 独立国际化服务层

---

## 2. 贸易与经济系统

### ✅ 已实现

- `TradeSystem.js` 已完成买入、卖出、燃料补给、旅行、跨星系跳跃判定、货物成本追踪、利润统计。
- `Economy.js` 已实现供需、价格漂移、市场深度、价格历史、经济周期、黑市价格、走私检查。
- `MarketUI.js` 已提供市场工作区编排和外壳 / 交易 / 资金 / 贸易站四个真实 render port；`MarketCommand.js` 冻结 UI / controller 契约，`MarketWorkspaceController.js` 统一解释领域动作。行情价格序列、快照、K 线/SVG 与冻结 Dashboard/K-line view model 只由无 DOM 的 `MarketChartPresenter.js` 生成，六个行情区域、两个单根 click 委托、商品行滚动、空态清理和 reset 解绑只由 `MarketChartViewAdapter.js` 持有，商品焦点/统计窗口/两视图局部重绘生命周期只由 `MarketChartController.js` 解释；商品卡与行情榜焦点校验、Context 来源和局部重绘已统一进入 `MarketSelectionController.js`。顶部 Chrome、详情地点/市场模式、引导高亮/滚动和动作落点已迁入 `MarketChromeController.js`，动态地点字段进入 HTML 前统一转义；现货摘要、黑市风险和灰市目录投影已迁入 `MarketSpotPresenter.js`，公开/黑市商品快照、现货外壳、Overview/Goods/Chart/Analysis 组合和远程地点/局部重绘端口已迁入 `MarketSpotController.js`，商品模型、卡片 HTML 与列表 command 协议已迁入 `MarketGoodsPresenter.js`，商品工具栏、列表、快速交易、键盘 intent 和 typed command 转换已迁入 `MarketGoodsController.js`，现金结构、贷款风险与本地贷款操作投影已迁入 `MarketCapitalPresenter.js`，贸易站领域快照与分区组合已迁入 `MarketOperationsPresenter.js`，批量计划、本地经营、商网概览与候选/已建站列表分别迁入 `MarketBatchPlanPresenter.js`、`MarketLocalOperationsPresenter.js`、`MarketOperationsOverviewPresenter.js`、`MarketTradeStationListPresenter.js`，资金/贸易站 DOM、typed command、经营排序和共享 Commerce 快照已迁入 `MarketFinanceController.js`，商品 Context 与共享 L4 详情投影已迁入 `MarketCommodityDetailPresenter.js`，其地点/市场模式/价格/供需/库存/现金解析与容器写入已迁入 `MarketCommodityController.js`；解锁路线由纯 `MarketExperienceRoute.js` 计算，一级/二级菜单、锁定回退、键盘焦点和 ARIA 由 `MarketWorkspaceNavigation.js` 独占，各地报价访问/价格/热度由纯 `MarketOverviewPresenter.js` 生成，表格 DOM、地点打开与价格口径键盘切换由 `MarketOverviewController.js` 独占。`MarketUI` 已由 1,512 行降至 337 行，不再直接查询或写入 DOM。商品焦点和经营排序不会再重绘无关区域，重复/非法排序不产生重绘，资金联动动作与纯策略动作分别使用不同 dirty-region 影响矩阵。
- `Trade.travelTo()` 已在旅行时联动推进 `Economy.advanceDay()`、`TradeStation.advanceDay()`、`Finance.advanceDay()`。

### 🔶 部分实现

- “市场深度”当前主要用于价格模型和指标展示，还不是一个可交互的订单簿或撮合系统。
- 商业终端虽然已经把现货、资本、经营整合到一起，但整体仍偏单机经营面板，没有更复杂的市场结构。

### ❌ 未实现

- 玩家间交易市场
- 拍卖行 / 委托单 / 订单簿撮合

---

## 3. 金融与资本系统

### ✅ 已实现

- `FinanceSystem.js` 已实现经营贷款、信用评级、贸易站追加投资和旧资本产品清算迁移。
- `MarketCapitalPresenter.js` 只投影现金结构、贷款风险、本地贷款动作与站点投资只读汇总；建站和追加投资统一归 `MarketOperationsPresenter.js`。
- 旧股票持仓、期货合约和手动保险会在首次载入旧存档时清算或退保；相关历史字段与结算逻辑仅作为兼容路径保留。
- 金融操作已通过 `GameManager -> CommerceFacade -> Finance` 接线完成，`CommerceFacade.getCommerceSnapshot()` 汇总贷款、信用与商网关键指标。

### 🔶 部分实现

- 资本工作区已有经营贷款与商网投入摘要，但仍缺少更高级的资产编组、区域资本策略和历史分析视角。

### ❌ 未实现

- 玩家间金融市场
- 新股票、期货和手动保险交易入口
- 委托交易 / 拍卖 / 订单撮合
- 更复杂的资产报表与历史分析系统

---

## 4. 贸易站与商网经营

### ✅ 已实现

- `TradeStationSystem.js` 已实现建站、升级、雇佣经理、策略切换、预计日收益、累计收益、候选站点筛选。
- `TradeStation.advanceDay()` 已接入旅行结算，站点会产生被动收益。
- `MarketUI.js` 编排“经营”工作区，`MarketOperationsPresenter.js` 只采集领域快照并组合分区；`MarketBatchPlanPresenter.js`、`MarketLocalOperationsPresenter.js`、`MarketOperationsOverviewPresenter.js`、`MarketTradeStationListPresenter.js` 分别拥有批量计划、本地经营、商网概览与候选/已建站列表，`MarketFinanceController.js` 统一解释其投资、升级、策略与排序 intent。选中商品、图表区间、子页、价格模式和经营排序已由无 DOM 的 `MarketWorkspaceSession.js` 持有，不再散落在 `MarketUI` 模块全局变量。
- `FinanceSystem.js` 已支持对站点做追加投资并收取分红。

### 🔶 部分实现

- `js/ui/TradeStationUI.js` 目前是旧入口兼容层，真实运行时能力已全部转发到 `MarketUI.js`。
- 贸易站/商网的主要交互已并入 `MarketUI.js`，但更深的跨站物流、调拨和区域供应链调度还未落地。

### ❌ 未实现

- 独立的贸易站专属界面模块
- 更复杂的商网物流模拟（仓储、跨站调拨、区域供应链调度）

---

## 5. 飞船、舰队与船员系统

### ✅ 已实现

- `FleetSystem.js` 已支持多船、席位、买船、卖船、切船、升级、改装件、自动派遣航线。
- `CrewSystem.js` 已支持船员市场、招募、分配/撤下、船员专长、日薪、成长与升级。
- 派遣航线、当前航段显示、残留飞线修复逻辑都已有实际运行链路。
- `FleetUI.js` 已提供船队、船员、升级、改装、招募等界面；机库主视图、船坞采购、船员详情、改装/保养详情与自动跑商策略/估算投影已分别迁入 `FleetHangarPresenter.js`、`FleetShopPresenter.js`、`FleetCrewPresenter.js`、`FleetModPresenter.js`、`FleetDispatchPresenter.js`。查看舰会话、机库根 DOM/Context/重绘后焦点与二级入口由 `FleetHangarController.js` 独占；采购根 DOM、购买 intent、采购焦点 diagnostics 与 reset 解绑由 `FleetShopController.js` 独占；船员 DOM、名单委托、危险解雇确认和 generation-safe 刷新由 `FleetCrewController.js` 独占，改装 DOM、引导焦点、五类 intent、危险售船确认和安全延迟刷新由 `FleetModController.js` 独占；派遣草案/诊断、表单 DOM/处理器与推荐/估算/Surface/命令用例分别由 `FleetDispatchSession.js`、`FleetDispatchViewAdapter.js` 和 `FleetDispatchController.js` 独占。
- `FleetSurfaceCoordinator.js` 独占 inline/blocking 活动面判定、`FleetInlinePortalController` 构造、危险确认只读上下文、统一关闭/reset 与冻结 diagnostics；Crew/Mod/Dispatch 只消费同一组 Surface 端口，`FleetUI` 不再持有 active surface 或 confirmation 状态。
- `FleetUI.js` 已由 1,236 行降至 237 行，不再读取 Hangar/Shop intent、绑定 `fleet-list/shop-list`、持有查看舰或采购焦点，也不再查询 `crew-*` / `mod-modal*` / `dispatch-*` DOM 或持有船员、改装、派遣 state 闭包；`FleetSurfaceCoordinator` 独占 Surface/确认/reset，并在内部组合 `FleetInlinePortalController` 处理 modal box 搬移/归还、ARIA/inert、滚动、Escape 与焦点，`FleetShipDetailController` 独占 Fleet/Crew selector 和 Context/L4 宿主，`FleetCommandAdapter` 独占 14 个 UI action 的 typed command 规范化。门面不读取不存在的全局 `GameManager`，公开渲染与三类弹层统一接收请求对象。内联详情关闭由 `GameUiCoordinator` 注入 latest-state Hangar 重绘 command；`FleetActionController` 按动作影响声明 `fleet-hangar` 或 `fleet-hangar + fleet-shop`，活动 Fleet 只更新对应 controller，隐藏 Fleet 不后台重绘。查看舰、采购焦点、改装/船员/派遣草案、inline/blocking surface mode、焦点、估算、命令、确认与迟到任务丢弃计数均进入冻结 diagnostics；关闭、会话 reset 和 Feature dispose 会统一解绑处理器并清空运行态。

### 🔶 部分实现

- 设计文档里的“飞船特殊技能树”“护盾/武器/乘客舱/外观改装”等高阶能力，目前只部分折算为船体数值、改装件和船员加成，没有形成完整子系统。
- 飞船高级玩法当前聚焦在货舱、燃料、船体、改装、派遣、船员；没有完整的战斗化或外观自定义化路线。

### ❌ 未实现

- 飞船战斗系统
- 乘客运输系统
- 外观涂装 / 标识自定义
- 独立的护盾 / 武器能源战斗模型

---

## 6. 星图、星系与探索系统

### ✅ 已实现

- `data/systems.js` 已生成多星系、多星球数据，并有等级解锁与跨星系旅行门槛。
- `StarmapRenderer.js` 已成为星图渲染门面，优先加载 `RendererThreeStarmap.js` 提供 Three.js 3D 星图，并在 WebGL2 不可用时使用 `Renderer2DStarmap.js` 降级；三层均实现 listener/Canvas/WebGL/Controls/纹理/几何体释放与可重复初始化，开发模式可用 `?starmap=2d` 运行真实降级 smoke。
- `MapUI.js` 已完成 3D 交互、星系/星球视图切换、悬停详情与可重复释放；星系总览模型与 HTML 已迁入 `MapGalaxyHubPresenter.js`，视图/悬停状态写入 `MapViewStateController.js`，POI 流程/探索 intent 迁入 `MapExplorationPresenter.js`，星球摘要/航线焦点/路线估算/travel intent 迁入 `MapPlanetDetailPresenter.js`，面板几何和统一 dataset intent 分别由 `MapPanelLayout.js`、`MapPanelController.js` 持有，探索档案/报告及两层 renderer 分别由 `MapSurveyDetailPresenter.js`、`MapSurveyDetailController.js` 持有，工作区请求、星系/星球切换、同/跨星系旅行分发、引导聚焦与 Renderer 对焦由 `MapNavigationController.js` 持有；市场入口和通用 Tab 状态机已迁出，`MapUI` 当前仅保留组合与公开转发。
- `GalaxyDataLayer.js` 已提供星系层次数据、星球运行时状态、批量状态更新接口，并参与存档读写。
- `ExplorationSystem.js` 已实现 POI 调查、秘密航线、勘探报告和经营决策情报。
- 探索档案 UI 已由 360 行 `ArchiveExplorationUI.js` 拆成 `ArchiveExplorationSession`、`ArchiveExplorationPresenter`、`ArchiveReportDetailPresenter` 与 `ArchiveExplorationController`；81 行兼容门面只组合航点/连续任务焦点、探索总览/报告详情纯投影、单一根节点检查/键盘/聚焦滚动和 Context/L4 adapter，并已接入 Archive reset/diagnostics。
- `MapUI.js` 只在 Context Inspector 的星球详情中接入 POI 与勘探摘要；已删除无 DOM 命中的独立探索终端按钮、浮层、EventBus 互斥、会话字段和孤儿 CSS，完整探索档案和单份报告由统一 L4 详情层承载。
- 探索进度已体现在已访问星球/星系、探索任务、探索成就、探索类胜利条件上。

### 🔶 部分实现

- 探索 MVP 已形成“POI -> 勘探报告 / 秘密航线”的闭环，但 POI 类型、遗迹链、隐藏区域和专属事件分支仍偏浅。
- 勘探报告已能转成市场、科研、航线、后勤等经营信号，并进入市场推荐、科研补给、舰队派遣评分和商网站点策略；后续缺口是更复杂遗迹链、隐藏区域和长线事件如何继续改变经营选择。
- 星球设施互动、长期星球状态变化还没有形成独立玩法层。

### ❌ 未实现

- 复杂遗迹链 / 隐藏区域链
- 星球设施互动
- 探索专属长线事件分支
- 基于勘探情报的更高阶商网 / 派遣自动编排

---

## 7. 派系、外交与黑市

### ✅ 已实现

- `FactionSystem.js` 已实现派系关系值、关系等级、星球控制归属、税率修正、黑市解锁条件。
- 交易行为已能影响派系关系，黑市访问与执法等级、走私检查相互联动。
- 派系 UI 已由 446 行 `FactionUI.js` 拆成 `FactionBoardPresenter`、`FactionDetailPresenter` 与 `FactionBoardController`；65 行兼容门面只组合关系总览/卡片/市场 CTA、Context/L4 和单一根节点委托，市场跳转后同步派系 Context 的旧行为被显式保留，并已接入 Archive reset/diagnostics。
- 黑市交易已经实装到 `TradeSystem.js` 与 `MarketUI.js`。

### 🔶 部分实现

- 外交行为当前主要是“通过交易 / 事件被动改关系”，缺少主动外交操作。
- 没有更高层的派系议程、联盟条约、敌对状态演变模拟。

### ❌ 未实现

- 主动外交选项（结盟、贿赂、谈判、宣战）
- 派系战争 / 区域控制变迁
- 长周期政治模拟

---

## 8. 科技研究系统

### ✅ 已实现

- `ResearchSystem.js` 已实现三选一科技抽取、前置校验、研究队列、研究推进、研究完成接力。
- 科技效果已经与经济、黑市视野、跨星系旅行等系统联动。
- 科研 UI 已由 716 行 `ResearchUI.js` 拆成 `ResearchBoardPresenter`、`ResearchDispatchPresenter`、`ResearchDetailPresenter` 与 `ResearchBoardController`；96 行兼容门面只组合三容器纯投影、两个稳定交互根、generation-safe 清空队列确认、Context/L4 adapter 与原有位置参数 API，并已接入 Archive reset/diagnostics。

### 🔶 部分实现

- 科技系统当前主要是菜单驱动与数值 / 规则解锁，尚未和探索设施、研究站点、专门科研事件形成更深的循环。

### ❌ 未实现

- 独立科研设施经营
- 科技蓝图发现 / 科研考古玩法
- 更复杂的分支科技树可视化

---

## 9. 随机事件系统

### ✅ 已实现

- `RandomEvent.js` 已实现事件权重、冷却、阶段分层、事件链、保护阈值、历史记录。
- `events.js` 已提供多选项事件文本与后果。
- `EventPresenter.js` 只生成冻结的事件标签、摘要、影响预览与选择项；`EventSurfaceController.js` 独占 Blocking DOM、键盘、焦点、提交防重和完整释放；66 行 `EventUI.js` 只负责 pending 会话与旧业务 API，不再渲染独立通知条。选择提交、session clear 和 application shutdown 均关闭弹层并释放 dismiss owner，DOM 缺失时 pending 保留。
- `RandomEventRuntimeController.js` 已统一按需加载、串行 roll、会话失效丢弃、pending 恢复、自动存档与失败重试，避免旧会话事件迟到提交。

### 🔶 部分实现

- 事件演出目前主要依赖文字和按钮，没有专门的对话 / 演出系统。
- 事件与星系状态、派系政治、探索点的深度联动还有限。

### ❌ 未实现

- 语音 / 演出化事件
- 事件 cutscene / 对话树系统
- 基于星区长期演化的动态世界事件

---

## 10. 任务、剧情与章节推进

### ✅ 已实现

- `QuestSystem.js` 已实现章节解锁、前置条件、推荐新手任务、进行中任务、完成判定、章节推进。
- 任务 UI 已由 1,279 行 `QuestUI.js` 拆成 `QuestWorkspaceSession`、Available/Active/Locked/Board/Route/Objective/Detail Presenter 与 `QuestBoardController`；可接取排序/简报、进行中进度/路线/操作、未解锁锁因/空态分别由三个生命周期 Presenter 独占，`QuestBoardPresenter` 从 519 行降至 208 行后只组合章节指挥台与分诊。98 行兼容门面只组合会话、纯投影、单一根节点委托、危险确认和 Context/L4 adapter。
- 教程完成后会通过日志推荐首批任务，并把下一步交给底部当前行动条，不再自动切换任务页。
- 任务已能监听旅行、交易、派系等动作。
- `DialogueSystem.js` 已实现轻量剧情内容层，可挂接教程尾声、指定任务简报 / 回信和章节推进过场，并支持少量分支选项对后续文本、任务推荐、奖励倾向和个别派系关系产生影响；UI 侧已拆为 `DialogueSession`、`DialoguePresenter`、356 行 `DialogueModalController` 与 29 行 `DialogueUI` 兼容门面，分别独占播放/分支状态、纯语义投影和 Surface/DOM/键盘/焦点；短视口离屏选项回显及完成/Escape 幂等提交已有自动化与真实浏览器覆盖。其中第二到第五章的部分关键主线已接入路线化奖励 / 关系修正，旧任务数据里的派系 ID 也已在 QuestSystem 内做兼容映射。

### 🔶 部分实现

- 剧情目前仍以任务描述、章节名、随机事件文本和少量对话过场为主，没有复杂剧情编排器。
- 没有专门的 NPC 关系系统、复杂对话分支管理器、长线剧情演出管理器。

### ❌ 未实现

- 分支剧情树
- NPC 好感 / 剧情线路系统

---

## 11. 成就、胜利与成长系统

### ✅ 已实现

- `AchievementSystem.js` 已实现成就检测、奖励发放、解锁通知。
- `VictorySystem.js` 已实现多路径胜利、路径进度展示、按章节逐步开放胜利路线。
- `ProgressionSystem.js` 已实现玩家等级、公司等级、升级奖励、新航线解锁提示。
- HUD 已展示净资产、经济周期、胜利路径进度等核心成长信息。

### 🔶 部分实现

- 成长奖励目前以数值和解锁为主，还没有更多结构性元进程系统。

### ❌ 未实现

- 声望头衔专属内容树
- New Game+ / 继承周目系统

---

## 12. 教程与新手引导

### ✅ 已实现

- `TutorialSystem.js` 已实现分步教程、UI 高亮、手动 / 动作触发推进、奖励、跳过、完成记忆。
- `TutorialStepPresenter`、`TutorialTooltipLayout` 与 `TutorialOverlayController` 已分别接入教程步骤/ARIA 纯投影、visual viewport/安全区/真实布局尺寸定位和 EventBus/定位安全高亮/L7 焦点循环生命周期；`TutorialUI.js` 已由 438 行收束为 25 行兼容组合门面。390×420/390×844 真实浏览器确认 fixed 高亮目标不位移、长 Tooltip 不重叠、视口高度变化自动重排，Shift+Tab/Tab 在动作首尾循环。
- `GuidanceSystem.js` 已实现非阻塞当前行动建议，覆盖初次交易、卖货目的地、初探宇宙、POI 调查等早期链路，并已接入待处理事件、低燃料补给、科研补给派遣预填、维修 / 资金阻塞解除、商网投入 / 批量经营、勘探情报跳转和资本侧贷款 / 股票 / 期货入口。
- `GuidanceActionController.js` 已承接行动条 actionType 分发，`GameManager` 只负责注入具体执行回调。
- `ActionGuideUI.js` 已实现固定展开的底部行动条、统一命令按钮和执行反馈。
- `CommandAction.js` 已统一市场、任务、探索、航行、机库等 CTA 的 surface / intent / verb 元信息。
- 教程完成状态会持久化到 `localStorage`。
- `MidgameTeachingChain.js` 已实现科研经济、自动跑商、贸易站经营和资金管理 4 条专题；无其他直接动作时由行动条提供主动启动入口，步骤只在派遣确认、建站、升级、还款等真实成功事件后推进。
- 自动跑商链复用每艘船的 `operatingStats.tradeCycles` 并记录启动基线，要求启动后完成 3 次经营循环。

### 🔶 部分实现

- 目前是线性脚本式新手引导，没有基于玩家行为的自适应教学。
- 行动建议已覆盖前 20-30 分钟的主要动作，并覆盖科研补给、普通商运派遣、维修、资金周转、商网建设 / 批量经营、勘探情报和资本侧风险等中期入口；专题教学链已闭合，后续重点是持续优先级回归和自动编排深度。

### ❌ 未实现

- 更高阶派遣 / 商网自动编排和长线经营目标提示

---

## 13. 存档、设置与数据管理

### ✅ 已实现

- `SaveSystem.js` 已实现 4 槽位存档（1 自动 + 3 手动）、导入导出、损坏槽位处理、运行时字段剔除、schema 逐版本迁移到 v17。
- `SaveWorkspacePresenter` 与 `SaveWorkspaceController` 已分别实现存档面板纯投影和删除/导入/导出交互；保存/读取只经 typed command 进入持久化事务，删除/成功导入重绘后恢复同槽位可用动作焦点，离屏时在设置主滚动区 nearest 回显；`SaveUI.js` 已由 447 行收束为 18 行请求对象组合门面，不再读取 DOM、直接访问存档系统或接收位置参数回调。763 行 `save-workspace.css` 是存档私有级联唯一 owner，旧样式文件相关选择器归零。
- `SettingsUiController` 已统一设置模块首击加载、并发去重、session/generation 失效、Blocking Surface、失败呈现与重试，并在模块加载前后永久持有唯一 launcher listener；reset 会作废迟到打开并清理模块局部会话，dispose 释放 launcher。设置控件/摘要纯模型与弹层分页/控件/命令反馈/危险确认/释放已分别迁入 `SettingsViewPresenter` 与 `SettingsModalController`，Feature manifest 新增 dispose hook；`SettingsManager.js` 已由 416 行降至 36 行组合门面，不再读取 DOM、直接写配置、调用 Renderer/Audio 或操作 Blob/EventBus。
- 星系数据层状态已纳入存档。
- `balanceMetrics` 已纳入存档，记录首单、继续经营、商品利润和长期路线等本地验收指标。
- `midgameChains` 已纳入存档，迁移和规范化会补全已知链、剔除未知链并保证至多一条活跃链。
- 正式设置命令通过 `UsageDataExportEffect` 生成脱敏导出，`gameVersion` 读取统一常量，导出格式使用独立的 `exportSchemaVersion`。

### 🔶 部分实现

- 存档介质仍是 `localStorage`，不是设计文档中的 `IndexedDB` / 云同步方案。
- 没有跨设备同步、云备份或账号体系。

### ❌ 未实现

- IndexedDB 存档
- 云存档 / 多端同步
- 账号登录后的在线档案管理

---

## 14. UI、交互与渲染层

### ✅ 已实现

- `HUD.js`、`MarketUI.js`、`FleetUI.js`、`MapUI.js`、`QuestUI.js`、`ResearchUI.js`、`FactionUI.js`、`AchievementUI.js`、`SaveUI.js`、`TutorialUI.js` 都已模块化；`GameShellProjection.js` 作为 Header、公司经营、Archive 角标与长期路线摘要的唯一组合入口，`HeaderStatusPresenter.js`、`CompanyOverviewPresenter.js` 与 `ArchiveBadgePresenter.js` 分别纯投影各自信息，`HudInteractionController.js` 独占 Shell 事件/弹层/星图工具与 dispose/re-init，`VictoryProgressPresenter.js` 纯渲染长期路线详情。`MarketExperienceRoute.js`、`MarketWorkspaceNavigation.js`、`MarketOverviewPresenter/Controller.js`、`MarketSpotPresenter/Controller.js`、`MarketChartPresenter/Controller.js`、`MarketGoodsPresenter/Controller.js`、`MarketSelectionController.js`、`MarketFinanceController.js` 与 `MarketCommodityDetailPresenter/Controller.js` 分别独占商业解锁模型、一级/二级菜单交互、各地价格表、现货组合、行情图表、商品列表/快速交易、商品卡/行情榜共享选择、资金/贸易站容器交互和商品 Context/L4；`FleetHangarPresenter/Controller.js`、`FleetShopPresenter/Controller.js`、`FleetCrewPresenter/Controller.js` 与 `FleetModPresenter/Controller.js` 分别独占机库、采购、船员和改装的纯投影及交互生命周期，自动跑商则由 `FleetDispatchPresenter/Session/ViewAdapter/Controller.js` 分别独占纯投影、草案诊断、DOM 与用例；`QuestWorkspaceSession`、Available/Active/Locked/Board/Route/Objective/Detail Presenter 与 `QuestBoardController` 分别独占任务候选焦点、三类生命周期、章节/分诊、路线、目标、详情和 DOM/危险确认生命周期，`QuestUI` 已由 1,279 行降至 98 行组合门面；`MarketWorkspaceSession.js`、`MarketWorkspaceEntrySession.js`、`MapWorkspaceSession.js` 与 `LogsWorkspaceSession.js` 分别独立承担商业内容选择/图表/排序、商业入口/浏览地点/待聚焦请求、星图局部选择/披露/航线焦点、通讯原始历史/筛选/聚合/未读会话。商业入口 DOM 由 `MarketWorkspaceEntryController.js` 协调，Archive/Fleet Tab 由 `WorkspaceTabController.js` 独占键盘/ARIA/深链，Map/Market/Fleet/Archive 的 Context → L4 入口已进入 local-scope `WorkspaceActionSlot.js`；Map 星球/POI 投影、面板几何和动作协议由 `MapPlanetDetailPresenter.js`、`MapExplorationPresenter.js`、`MapPanelLayout.js` 与 `MapPanelController.js` 持有，日志列表 DOM/控件/Context 由 `LogsWorkspaceController.js` 协调并支持完整释放/重建。
- Fleet 二级界面的 Surface/确认生命周期已进入 `FleetSurfaceCoordinator.js`，`FleetUI.js` 只组合 controller 图、latest-state 重绘入口与兼容公开 API。
- `ResearchBoardPresenter`、`ResearchDispatchPresenter`、`ResearchDetailPresenter` 与 `ResearchBoardController` 分别独占科研总览/候选/队列/完成、补给/阻塞、Context/L4 和 DOM/清空确认生命周期；`ResearchUI` 已由 716 行降至 96 行组合门面，并由 Archive Feature 统一 reset 和公开 diagnostics。
- `FactionBoardPresenter`、`FactionDetailPresenter` 与 `FactionBoardController` 分别独占派系总览/关系卡/市场 CTA、Context/L4 和单一根 DOM/键盘/市场跳转生命周期；`FactionUI` 已由 446 行降至 65 行组合门面，并由 Archive Feature 统一 reset 和公开 diagnostics。
- `AchievementBoardPresenter`、`AchievementDetailPresenter` 与 `AchievementBoardController` 分别独占成就总览/分类/奖励池/卡片、Context/L4 和单一根 DOM/键盘生命周期；`AchievementUI` 已由 318 行降至 63 行组合门面，并由 Archive Feature 统一 reset 和公开 diagnostics。
- `ArchiveExplorationSession`、`ArchiveExplorationPresenter`、`ArchiveReportDetailPresenter` 与 `ArchiveExplorationController` 分别独占航点/连续任务焦点、探索总览/报告卡/连续任务、Context/L4 和单一根 DOM/键盘/聚焦滚动生命周期；`ArchiveExplorationUI` 已由 360 行降至 81 行组合门面，并由 Archive Feature 统一 reset 和公开 diagnostics。
- `SaveWorkspacePresenter` 与 357 行 `SaveWorkspaceController` 分别独占存档安全/槽位/迁移/确认描述纯投影和单一根 DOM/确认/导入导出/异步失效/重绘后槽位焦点生命周期；`SaveCommand` 与 `SaveCommandAdapter` 分别独占保存/读取冻结契约和 UI intent 转换，`SaveUI` 已由 447 行降至 18 行请求对象组合门面，存档 Feature dispose 与 `GameUiCoordinator.reset()` 会统一清理并公开 diagnostics。763 行 `save-workspace.css` 独占存档私有级联，旧 owner 命中归零；1280×720、390×844 迁移前后计算样式/几何一致，360×640 确认单一内部滚动、零横向溢出、44px 控件及同槽位焦点回显。
- 1,341 行 `settings-workspace.css` 已成为设置 launcher、弹层、分页、控件、延迟状态、焦点和响应式布局的唯一 owner；`settings/responsive/interstellar-trader/surfaces/bridge-responsive` 中 257 个有效私有选择器分支归零，11 个无消费者旧类删除。1280×720 数据页迁移前后计算样式/几何一致；360×640 摘要由单列改为 2×2，内容可视高度增加约 124px，并通过单一内部滚动、零横向溢出、44px 控件、方向键分页与关闭焦点返回验收。
- `TutorialStepPresenter`、`TutorialTooltipLayout` 与 `TutorialOverlayController` 分别独占教程内容/ARIA、视口定位/监听和 EventBus/高亮/L7 焦点生命周期；`TutorialUI` 已由 438 行降至 25 行组合门面，Feature dispose 会完整 destroy。高亮只为 computed static 目标临时补定位类，Layout 在 Overlay 可见后以 offset 布局尺寸定位，重复 init/destroy 不叠加焦点陷阱 listener。
- `DialogueSession`、`DialoguePresenter` 与 `DialogueModalController` 分别独占剧情播放/分支状态、进度/摘要/选项纯投影和 Blocking Surface/DOM/键盘/焦点生命周期；`DialogueUI` 已由 384 行降至 29 行组合门面，Runtime/Feature dispose 均走幂等释放。360×640 与 390×844 真实浏览器验收确认长文本与四选项共用单一内容滚动，End 聚焦最后选项后完整可见，Escape 完成回调只在弹层隐藏/会话重置后提交一次。
- `WorkspaceContextAdapters.js` 已把 archive 五类 Context key 映射到独立 detail type；档案分类切换会关闭旧 L4 并清除跨分类 Context，Escape/关闭恢复原触发点而不退出 archive 工作区。
- `GameUiCoordinator` 已由 680 行降至 377 行，只保留 dirty-region 路由、Feature ensure/reset、全局非工作区刷新与 diagnostics 组合；306 行 `GameUiWorkspaceRenderer` 独占 Market/Fleet/Archive/Save 的 latest-state 请求、局部 renderer、Context adapter 与单一 typed command 注入，87 行 `GameUiRenderSession` 独占成功区域计数和刷新事务。旧 `hud` dirty region 已升级为 canonical `shell`；`workspaceSessions` 只读公开 map/trade/fleet/archive/logs，隐藏工作区不后台重绘，会话替换会清空局部选择与失效 Context/L4。
- `NavigationController.reset()` 会在会话替换时一次清空当前与隐藏 workspace 的全部 detail stack，同时保留玩家当前 L3 目的地；旧存档详情不再等到重新进入工作区时才被延迟关闭。canonical active workspace、各工作区冻结 L4 栈和深度已直接进入 UI application 顶层 diagnostics。
- `UIManager.js` 已接管底栏导航与 latest-state 工作区进入；`WorkspaceSurfaceController.js` 将 map/trade/fleet/archive/logs 作为 `#game-main` 的五个同级 `.workspace-surface`，统一投影 `is-active`、`data-workspace-active`、`inert`、ARIA 和来源相关焦点，其可见 Surface ID、一致性、激活与焦点计数已直接进入顶层 diagnostics。市场已脱离星图 DOM，商业入口与内部 Tab 分别由明确 controller 持有；MapUI 只通过生命周期注入的窄 navigation action 请求切换，不再导出旧市场/Tab API、监听底栏、查询通用 Tab DOM、发布全局 manager facade 或直接操作 L3 DOM，`SurfaceManager` 也已删除 primary/secondary workspace 兼容层。
- `WorkspaceDetailSurface` 的 workspace/depth/open、renderer type 与开合/渲染计数，以及 `SurfaceManager` 的可见 Blocking ID、顶层 Surface、注册/活动 Escape layer 优先序和绑定计数，均以冻结可序列化快照进入 UI application 顶层 diagnostics；两者不暴露 DOM、回调或领域 state。
- 当前运行时渲染主线是 `StarmapRenderer.js -> RendererThreeStarmap.js`，`Renderer2DStarmap.js` 作为 WebGL2 降级路径保留。
- 市场页已演化为统一“商业终端”，整合现货、资本、经营三套入口。
- 经营 UI 密度首批已降级：全局下一步归行动条，HUD / 市场 / 地图 / 机库主要展示局部状态、信号、可升级项和本页动作。
- `HUD.js` 从 787 行进一步降至 125 行，不再持有 Header/公司/Archive Badge 投影、长期路线 HTML 或无调用的任务动作端口；只保留交互生命周期与通讯日志。`GameShellProjection` 以单次事务更新 Header/公司/Archive/长期路线摘要，`ActionPresentation` 使用 `shell` 而非 `hud`；`HudInteractionController` 同时独占 Context `900px` 断点 listener，并在 dispose/re-init 时释放/重建；不可逆长期路线选择仍由它发布 typed action，再由 `VictoryRuntimeController.choosePolicy()` 统一提交消息、舰队同步、任务进度、UI 失效和行动引导刷新。
- `LogMessage.js` 已成为通讯日志 `text/type/source` envelope 与 17 类来源标签的唯一契约；动作、日结、引导、存档、设置、教程、成就和胜利运行时显式发布领域来源，`LogsWorkspaceSession` 分离来源筛选与机会/风险信号筛选，聚合要求来源、类型、正文同时一致，列表、Context 与 L4 共用同一来源投影，不再从正文猜测任务或科研类别。
- `ArchiveCommand.js` 与 `ArchiveCommandAdapter.js` 已成为任务、科研、派系 12 类 UI 动作的冻结契约和唯一 intent 转换边界；`ArchiveActionController.handleCommand` 统一规范化/拒绝/路由，Quest/Research/Faction 门面只接收请求对象和单一 `onCommand`，`GameUiWorkspaceRenderer` 与 `GameUiApplicationRuntime` 不再展开领域回调。
- `SaveCommand.js` 与 `SaveCommandAdapter.js` 已成为保存/读取两类 UI 动作的冻结契约和唯一 intent 转换边界；`GamePersistenceController.handleCommand` 统一规范化、拒绝和路由，`SaveUI`、`GameUiWorkspaceRenderer` 与 `GameUiApplicationRuntime` 只保留请求对象和单一 `onCommand`。删除与 JSON 导入导出继续由 Save Workspace 本地 effect owner 承担。
- CSS 已拆分为布局、面板、地图、状态、响应式等多个文件；当前共 29,092 行。生产级联中的 Header、Bottom Nav、Action Guide、Starmap Controls、Market、Settings 与 Save Workspace 均已形成明确 owner；Context Inspector、Workspace Detail、Starmap Controls、Settings 与 Save Workspace 分别进入独立模块，使 Global Shell 从 789 行降到 72 行并只保留跨层关系。3,193 行 Market 基础规则及 497 个有效 legacy 选择器分支已统一收口到 `market-terminal.css`，`fleet/panels/systems/responsive/interstellar-trader` 五个旧 owner 的 Market 私有选择器归零；Settings 与 Save Workspace 私有选择器也分别迁入 `settings-workspace.css`、`save-workspace.css` 并从旧 owner 归零。旧主题、旧响应式、Modal、Layout 中 162 个 Header 规则、97 个 Bottom Nav 规则/混合选择器、423 行 Action Guide/Command Slot、1,425 行旧星图控制轨/并行 HUD 小窗、724 行退役 Company Directives UI 及 566 行旧市场面板/重复规则已删除，并由静态回流测试、存档兼容测试及浏览器验收保护；窄屏空态 Inspector、市场标题栏、设置/存档工作区、Terminal Header 与事件 flow surface 的几何回归也已纳入同一 owner。

### 🔶 部分实现

- `TradeStationUI.js` 仍是空壳。
- 当前视觉与交互已可玩，但离设计文档中的完整视觉体系、音画反馈、一致化组件库还有距离；`1280 × 720` 与 `390 × 844` 五工作区主路径已完成真实浏览器走查，仍需保留五视口发布矩阵和更多深层状态截图回归。
- Canvas 2D fallback 已存在，但视觉细节和交互能力低于 Three.js 主线，仍需保持基本功能契约一致。

### ❌ 未实现

- 独立贸易站 UI
- 资源化 / 分组化音频反馈层
- 完整设计系统 / 组件规范化输出

---

## 15. 音效与音乐系统

### ✅ 已实现

- `AudioManager.js` 提供轻量短提示音播放，使用 Web Audio 程序化合成，不依赖外部资源。
- 设置面板已接入音效开关与音量控制，设置会持久化到 `linegame_settings`。
- UI 点击、交易成交、航行和事件提醒已接入首批音效反馈。

### 🔶 部分实现

- 当前仅覆盖短反馈音 MVP；还没有背景音乐、音频资源加载、动态音乐或分组混音总线。

### ❌ 未实现

- BGM 播放系统
- 动态音乐
- 3D 音频定位
- 音量分组
- 音频资源管理

---

## 16. 多人游戏与网络功能

### ✅ 已实现

- 无网络 / 多人模块代码。

### 🔶 部分实现

- 无。当前项目仍是纯前端单机状态。

### ❌ 未实现

- 服务器 / 客户端网络架构
- WebSocket / 实时同步
- 好友、公会、聊天
- 玩家交易、排行榜、多人事件
- 存档同步和联网会话

---

## 17. 本地化与国际化

### ✅ 已实现

- 当前文本与界面主要是中文内容，个别显示格式已显式使用 `zh-CN`。

### 🔶 部分实现

- 现状只是“中文硬编码可玩”，还不属于可切换语言的 i18n 系统。

### ❌ 未实现

- 语言包 / 翻译字典
- 文本外置与键值检索
- 语言切换
- 多字体 / 排版适配
- 文化与合规适配层

---

## 18. 工程化与测试

Context Inspector 已从 490 行混合模块拆为 36 行兼容门面、179 行 Session、26 行纯 Presenter、302 行 ViewAdapter 与 307 行 Controller；不可变 key/常规与紧凑视口开合/revision、壳层/空态、DOM/ARIA/宿主/事件/焦点、renderer/action/Escape/响应式协调分别只有一个 owner，Context/renderer workspace、计数、开合与 ViewAdapter 生命周期以冻结可序列化 diagnostics 汇入 UI application 顶层快照且不暴露领域对象或 DOM。星图 UI 同样已把面板视图、Context/Escape、交互绑定生命周期与导航用例拆成独立 controller，组合门面不再直接写 DOM 或全局回调。

### ✅ 已实现

- `tests/` 下已有 233 个测试文件、1581 项测试，覆盖统一动作控制器图、成就与成就检查队列、持久化事务、自动贸易、黑市、商业终端与 Game Shell/Header/Company/Archive Badge Presenter、信息所有权与 Header/Bottom Nav/Action Guide/Context/Detail/Starmap Controls/Market CSS owner、退役 Company Directives UI 与旧存档兼容、Map Galaxy Hub/View State/Survey Detail/Panel Layout/Panel View/Context/Interaction/Navigation Controller/Workspace Surface/Detail 生命周期与 L3 CSS 所有权/UI Navigation port/Application diagnostics/Market Chrome/Chart Presenter/View Adapter/Controller/Selection/Spot/Goods/Finance/Commodity/Capital/Operations/Batch Plan/Local Operations/Operations Overview/Trade Station List/Commodity Detail/Experience Route/Overview/Workspace Navigation Controller/Fleet Hangar/Shop/Crew/Mod/Dispatch Presenter/Session/View Adapter/Controller/Surface Coordinator/Inline Portal/Ship Detail/Command Adapter、Quest Available/Active/Locked/Board/Route/Objective/Detail Controller/Presenter/Session、Research Board/Dispatch/Detail Controller/Presenter、Faction Board/Detail Controller/Presenter、Achievement Board/Detail Controller/Presenter、Archive Exploration Session/Board/Report Detail/Controller、Settings View/Modal/Launcher Controller、Save Workspace Presenter/Controller/Command Adapter/CSS owner、Tutorial Step/Tooltip/Overlay、Dialogue Session/Presenter/Modal Controller、Ship Detail/Workspace Object Detail Presenter、typed market/fleet/archive/save/settings command、Context 局部 intent、常规/紧凑视口开合隔离与档案分页生命周期、船员、经济、派系、经营金融、舰队、科研任务档案动作编排、Game Application shell/Runtime Graph/五个职责节点工厂簇/启动投影/Feature/UI/Guidance/Loop Runtime 组合边界与 manifest/registry 生命周期、延迟工作区局部失败恢复、Renderer dispose/re-init/开发态 2D 强制降级、Shell/HUD/FleetUI 命令所有权、剧情/随机事件/胜利运行时、eager UI 壳/设置/首次进入与释放生命周期、教学引导与 onboarding policy、存档、贸易、航行、探索、事件、自动派遣与日结算提交顺序、命令目的地、行动引导执行适配、市场工作区入口、工作区 Tab、局部操作槽、会话生命周期、时钟、Market/Fleet/Archive 内部增量失效、typed 日志来源契约、日志 Context/筛选/聚合和 UI 所有权。
- 存档契约、经济、随机事件、商网 / 金融等核心逻辑已有较多单测。

### 🔶 部分实现

- 当前以单元 / 集成为主，没有看到完整的浏览器端 E2E 回归链路。
- 仓库已有 Cloudflare Pages 构建与部署 workflow，当前依次执行 `npm ci`、`npm test` 和 `npm run build`，凭据完整时才部署。

### ❌ 未实现

- 浏览器端 E2E 自动化
- 联网 / 部署后的集成验证流水线

---

## 19. 结论

如果按“当前能不能玩、核心系统是否接线”来判断，项目的大部分核心玩法已经实现。

如果按设计文档的完整蓝图来判断，当前最主要的“部分实现 / 未实现”集中在：

1. 探索深化不足：探索 MVP 已形成 POI / 秘密航线 / 勘探报告闭环，但更复杂的遗迹链、隐藏区域、专属事件分支和经营联动仍未展开。
2. 飞船高阶玩法不足：已有舰队 / 改装 / 船员，但没有战斗、乘客、外观等大系统。
3. 剧情演出不足：已有章节任务和事件文本，但没有对话树 / 剧情演出引擎。
4. 远期平台能力仍未做：背景音乐、音频资源包、动态音乐、多人、i18n、云存档都还没开始；基础短音效 MVP 已经接入。

### 建议的文档口径

- 对外可以明确表述为：“核心单机贸易经营版与中期专题教学已实现；基础短音效已接入；探索深化、完整音频平台、多人、国际化属于后续阶段。”
- 不再使用旧文档里 `Renderer.js`、`IndexedDB 已实现`、`TradeStationUI 独立可用` 这类已经过时的说法。
